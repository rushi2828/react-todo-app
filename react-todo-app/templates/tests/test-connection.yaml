apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "react-todo-app.fullname" . }}-test-connection"
  labels:
    {{- include "react-todo-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "react-todo-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
